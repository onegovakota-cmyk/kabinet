# v9 — исправление книг и таймера

Исправлено:
- полки «Читаю / Хочу прочитать / Прочитано / Отложено / Купленные / Любимые»;
- после сохранения книга автоматически открывается на своей полке;
- переключение полок работает через устойчивый общий обработчик;
- таймер теперь доступен для любой незавершённой книги;
- если запустить таймер для «Хочу прочитать» или «Отложено», книга автоматически переходит в «Читаю»;
- таймер продолжает считать время после сворачивания приложения;
- после возврата в приложение отображение времени сразу обновляется;
- добавлена понятная подпись состояния таймера.

SQL запускать не нужно — база данных не менялась.

На GitHub заменить:
1. index.html
2. style.css
3. app.js
4. service-worker.js

После Commit changes дождаться GitHub Pages и полностью перезапустить приложение.
